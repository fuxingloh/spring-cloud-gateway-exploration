dependencies {
    implementation 'org.springframework.cloud:spring-cloud-starter-gateway'
    implementation 'redis.clients:jedis:3.4.1'
}

bootBuildImage {
    imageName = "spring-gateway.fuxing.dev/gateway-jedis:latest"
}
